import { useAuth } from '../context/AuthContext';
import { useLocation } from 'react-router-dom';

export default function ProfileCompletionModal() {
  // This component is no longer needed - replaced with gentle prompts
  return null;
}
